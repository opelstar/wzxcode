> This documentation is generated by [JApiDocs](https://japidocs.agilestudio.cn/).
---
# 刷写：刷写控制类
## 开始刷写接口

*作者: lwx1099797*

**请求URL**

/bus/startBrush `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string //通道号 can/canfd 时有值  M 0 [0-15]",
	"ip":"string //ECU部件ip",
	"isAccessFlag":"string //部件连接接入认证标识，0-长安、北汽GW TLS认证 1-长安 AIS部件接入认证 2-不需要接入认证",
	"loopCount":"string //循环次数",
	"ecuLoopCount":"string //循环次数",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"flowIds":"long[]",
	"isContinue":"string //场景测试 1：错停 :0：错顺"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 停止刷写接口

*作者: lwx1099797*

**请求URL**

/bus/stopBrush `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string //通道号 can/canfd 时有值  M 0 [0-15]",
	"ip":"string //ECU部件ip",
	"isAccessFlag":"string //部件连接接入认证标识，0-长安、北汽GW TLS认证 1-长安 AIS部件接入认证 2-不需要接入认证",
	"loopCount":"string //循环次数",
	"ecuLoopCount":"string //循环次数",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"flowIds":"long[]",
	"isContinue":"string //场景测试 1：错停 :0：错顺"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 添加流程接口

*作者: lwx1099797*

**请求URL**

/bus/addProcesses `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除流程接口

*作者: lwx1099797*

**请求URL**

/bus/deleteProgress `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过场景id查询流程接口

*作者: lwx1099797*

**请求URL**

/bus/querryProcessesByScenarioId `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过id查询流程详情接口

*作者: lwx1099797*

**请求URL**

/bus/querryProcessesDetail `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 更改循环次数接口

*作者: lwx1099797*

**请求URL**

/bus/updateLoopCount `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 更改延时参数接口

*作者: lwx1099797*

**请求URL**

/bus/updateInterval `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 自定义编辑接口

*作者: lwx1099797*

**请求URL**

/bus/editProcess `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 重置文件相关服务

*作者: lwx1099797*

**请求URL**

/bus/resetFileRelatedProcess `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除步骤接口

*作者: lwx1099797*

**请求URL**

/bus/deleteProcess `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取响应接口

*作者: lwx1099797*

**请求URL**

/bus/getResponseByProcessId `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过xml执行流程接口

*作者: lwx1099797*

**请求URL**

/bus/excuteXmlBrush `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过xml执行流程接口

*作者: lwx1099797*

**请求URL**

/bus/doXmlBrush `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 新增场景接口

*作者: lwx1099797*

**请求URL**

/bus/addView `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long",
	"scenarioId":"long[]",
	"name":"string",
	"status":"string //场景的状态",
	"createTime":"date //创建时间",
	"updateTime":"date //更新时间",
	"children":[{
		"id":"long",
		"scenarioId":"long //场景ID",
		"ecuId":"long //部件ID",
		"ecuName":"string //部件名称",
		"tagId":"long //部件ID",
		"name":"string //流程名称",
		"flowTemplateContent":"string //流程模板详情",
		"status":"string //流程的状态 0:未執行 1成功 2 失敗 3 未執行 5執行暫停",
		"createTime":"date //创建时间",
		"updateTime":"date //更新时间",
		"ord":"int",
		"isContinue":"string //场景测试 1：错停 :0：错顺"
	}],
	"order":"int //顺序"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询场景接口

*作者: lwx1099797*

**请求URL**

/bus/queryView `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除场景接口

*作者: lwx1099797*

**请求URL**

/bus/deleteViewById `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long",
	"scenarioId":"long[]",
	"name":"string",
	"status":"string //场景的状态",
	"createTime":"date //创建时间",
	"updateTime":"date //更新时间",
	"children":[{
		"id":"long",
		"scenarioId":"long //场景ID",
		"ecuId":"long //部件ID",
		"ecuName":"string //部件名称",
		"tagId":"long //部件ID",
		"name":"string //流程名称",
		"flowTemplateContent":"string //流程模板详情",
		"status":"string //流程的状态 0:未執行 1成功 2 失敗 3 未執行 5執行暫停",
		"createTime":"date //创建时间",
		"updateTime":"date //更新时间",
		"ord":"int",
		"isContinue":"string //场景测试 1：错停 :0：错顺"
	}],
	"order":"int //顺序"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 更新场景

*作者: lwx1099797*

**请求URL**

/bus/updateView `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long",
	"scenarioId":"long[]",
	"name":"string",
	"status":"string //场景的状态",
	"createTime":"date //创建时间",
	"updateTime":"date //更新时间",
	"children":[{
		"id":"long",
		"scenarioId":"long //场景ID",
		"ecuId":"long //部件ID",
		"ecuName":"string //部件名称",
		"tagId":"long //部件ID",
		"name":"string //流程名称",
		"flowTemplateContent":"string //流程模板详情",
		"status":"string //流程的状态 0:未執行 1成功 2 失敗 3 未執行 5執行暫停",
		"createTime":"date //创建时间",
		"updateTime":"date //更新时间",
		"ord":"int",
		"isContinue":"string //场景测试 1：错停 :0：错顺"
	}],
	"order":"int //顺序"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 更改场景顺序接口

*作者: lwx1099797*

**请求URL**

/bus/changeScenarioOrd `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long",
	"scenarioId":"long[]",
	"name":"string",
	"status":"string //场景的状态",
	"createTime":"date //创建时间",
	"updateTime":"date //更新时间",
	"children":[{
		"id":"long",
		"scenarioId":"long //场景ID",
		"ecuId":"long //部件ID",
		"ecuName":"string //部件名称",
		"tagId":"long //部件ID",
		"name":"string //流程名称",
		"flowTemplateContent":"string //流程模板详情",
		"status":"string //流程的状态 0:未執行 1成功 2 失敗 3 未執行 5執行暫停",
		"createTime":"date //创建时间",
		"updateTime":"date //更新时间",
		"ord":"int",
		"isContinue":"string //场景测试 1：错停 :0：错顺"
	}],
	"order":"int //顺序"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 更改流程顺序接口

*作者: lwx1099797*

**请求URL**

/bus/changeProcessTemplateOrd `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 显示响应接口

*作者: lwx1099797*

**请求URL**

/bus/displayDiagnosis `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导出场景接口

*作者: lwx1099797*

**请求URL**

/bus/exportScenarios `POST` 

**请求体**

```json
{
	"processTemplateIdList":"long[] //导出流程id集合",
	"path":"string //导出路径",
	"importPath":"string //导入路径"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导入场景接口

*作者: lwx1099797*

**请求URL**

/bus/importScenarios `POST` 

**请求体**

```json
{
	"processTemplateIdList":"long[] //导出流程id集合",
	"path":"string //导出路径",
	"importPath":"string //导入路径"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询过程和结果信息接口

*作者: lwx1099797*

**请求URL**

/bus/querryProcessAndResultInfo `POST` 

**请求体**

```json
{
	"processTemplateIdList":"long[] //导出流程id集合",
	"path":"string //导出路径",
	"importPath":"string //导入路径"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 在场景测试中，修改流程关联的部件

*作者: lwx1099797*

**请求URL**

/bus/changeEcuName `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"tagId":"long //选中的流程id",
	"scenarioId":"long //选中的场景id",
	"name":"string //输入的流程部件名",
	"scenarioName":"string //输入的场景名"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 在场景测试中，修改场景名

*作者: lwx1099797*

**请求URL**

/bus/changeScenarioName `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"tagId":"long //选中的流程id",
	"scenarioId":"long //选中的场景id",
	"name":"string //输入的流程部件名",
	"scenarioName":"string //输入的场景名"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 报文镜像：Mirroring镜像控制层
## 激活bus Mirroring镜像

**

**请求URL**

/bus/openBusMirroring `POST` 

**请求体**

```json
{
	"manfac":"string【必须】",
	"manfacId":"string【必须】",
	"canList":[{
		"canName":"string",
		"canId":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭bus Mirroring镜像

**

**请求URL**

/bus/closeBusMirroring `POST` 

**请求体**

```json
{
	"manfac":"string【必须】",
	"manfacId":"string【必须】",
	"canList":[{
		"canName":"string",
		"canId":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭bus Mirroring镜像

**

**请求URL**

/bus/queryBusMirroringStart `POST` 

**请求体**

```json
{
	"manfac":"string【必须】",
	"manfacId":"string【必须】",
	"canList":[{
		"canName":"string",
		"canId":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取can路集合

**

**请求URL**

/bus/getCanList `POST` 

**请求体**

```json
{
	"manfac":"string【必须】",
	"manfacId":"string【必须】",
	"canList":[{
		"canName":"string",
		"canId":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 配置BUSSMIORRING实时保存接口

**

**请求URL**

/bus/configReamTimeSave `POST` 

**请求体**

```json
{
	"isRealTimeStore":"string //1:yes,0:no【必须】",
	"realTimeStorePath":"string //实时保存的路径【必须】",
	"realTimeStoreName":"string //实时保存报文文件名称",
	"realTimeStoreSize":"string //实时保存单个报文文件大小 MB"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 配置BUSSMIORRING实时保存接口

**

**请求URL**

/bus/getReamTimeConfig `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：can接口卡配置
## can接口卡配置存储

**

**请求URL**

/can/save `POST` 

**请求体**

```json
{
	"udsSysCanPhysCardType":"string //0 pcan, 1 zlg【必须】",
	"linkDevSubType":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## can接口卡配置获取

**

**请求URL**

/can/findAll `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## can接口卡子类配置获取

**

**请求URL**

/can/findSubType `POST` 

**请求体**

```json
{
	"udsSysCanPhysCardType":"string //0 pcan, 1 zlg【必须】",
	"linkDevSubType":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询周期性激活配置

**

**请求URL**

/can/findLinkSendPeriodCfg `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 修改周期性激活配置

**

**请求URL**

/can/saveLinkSendPeriodCfg `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long //持久化数据标识",
	"idx":"int //key",
	"canID":"int",
	"period":"int",
	"count":"int //send count; count == 0 send forever, count != 0 send count msg and stop",
	"data":"string //16进制 8位 10 e4 11 12 13 14 15 16",
	"active":"string //发送激活状态 0发送 1不发送"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除周期性激活配置

**

**请求URL**

/can/deleteLinkSendPeriodCfg `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long //持久化数据标识"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 刷写：证书灌装
## 证书灌装准备步骤

**

**请求URL**

/cert/certPrepare `GET` `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"operationType":"string //操作类型 write灌装 update更新【必须】",
	"channel":"string //频道"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取证书种子文件

**

**请求URL**

/cert/getCertReq `GET` `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"operationType":"string //操作类型 write灌装 update更新【必须】",
	"channel":"string //频道"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取证书种子文件

**

**请求URL**

/cert/beginWriteCert `GET` `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"operationType":"string //操作类型 write灌装 update更新【必须】",
	"certPath":"string //证书路径【必须】",
	"channel":"string //频道"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭3e线程

**

**请求URL**

/cert/closeThreadPool `GET` `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 高压下电

**

**请求URL**

/cert/highVolPowerOff `GET` `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //频道【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 日志采集：ADAS 控制层
## 38服务文下载

**

**请求URL**

/logCollection/transferUpload `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"filePath":"string //保存路径",
	"batchDownload":[{
		"id":"long",
		"path":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导入日志接口（excel表格）

**

**请求URL**

/logCollection/importLogDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"logFileDirPath":"string //日志路径excel"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取文件夹列表接口

**

**请求URL**

/logCollection/queryLogDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取文件夹列表下子文件接口

**

**请求URL**

/logCollection/queryLogDirChildren `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"parentID":"long",
	"pageSize":"int",
	"pageNum":"int",
	"path":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除文件

**

**请求URL**

/logCollection/deleteDirById `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long //父Id",
	"idList":"long[] //删除id列表"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## getCsvPath

**

**请求URL**

/logCollection/getCsvPath `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## addLog

**

**请求URL**

/logCollection/addLog `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## stopDownload

**

**请求URL**

/logCollection/stopDownload `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 导出日志
## 导出日志模板

**

**请求URL**

/currency/exportExcel `GET` 


**返回结果**

```json
{}
```
# 日志采集：F项目日志采集控制层
## F项目日志采集

**

**请求URL**

/downCollection/Upload `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"batchFileName":[{
		"path":"string"
	}],
	"fileAddress":"string //文件储存地址"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## F项目日志采集
下载状态

**

**请求URL**

/downCollection/statusQuery `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除接口

**

**请求URL**

/downCollection/deleteDirById `POST` 

**请求体**

```json
{
	"idList":"int[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：配置控制层
## 导入ODX文件

**

**请求URL**

/ecu/config/importOdx `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|ODX文件

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 上传dll文件

**

**请求URL**

/ecu/config/uploadDll `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|dll文件

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 上传dll文件

**

**请求URL**

/ecu/config/uploadEcuCert `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|dll文件
manfac|string|是|车型名称
manfacId|string|是|车型Id

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 上传dll文件

**

**请求URL**

/ecu/config/uploadEcuCertKey `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|dll文件
manfac|string|是|车型名称
manfacId|string|是|车型Id

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# TODO
## 第一步

*作者: zWX929034*

**请求URL**

/auto/config/step1 `POST` 

**请求体**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 第二步

*作者: zWX929034*

**请求URL**

/auto/config/step2 `POST` 

**请求体**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 其他：控制器
## 保存数据

**

**请求URL**

/data/save `POST` 


**返回结果**

```json
string{}
```
## 获取报文日志

**

**请求URL**

/data/findAll `POST` 


**返回结果**

```json
Iterable{}
```
## 查询报文日志

*作者: yWX970191*

**请求URL**

/data/queryDataGramMonitor `POST` 


**返回结果**

```json
Iterable{}
```
## 获取执行日志

**

**请求URL**

/data/queryExecuteStep `POST` 


**返回结果**

```json
[{
	"id":"long",
	"methodName":"string //方法名称",
	"createTime":"date //请求时间",
	"content":"string //请求内容",
	"type":"string //1:request,2:response"
}]
```
## 根据id增量查询

*作者: yWX970191*

**请求URL**

/data/findById `POST` 


**返回结果**

```json
string{}
```
# 报文镜像：dbc管理控制层
## 导入DBC文件

**

**请求URL**

/dbc/manage/importDbc `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|DBC文件

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导入DBC文件

**

**请求URL**

/dbc/manage/importDbc `POST` 

**请求体**

```json
{
	"filePath":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据canId和data获取信号

**

**请求URL**

/dbc/manage/findSignalByCanIdAndData `POST` 

**请求体**

```json
{
	"canId":"string【必须】",
	"data":"string【必须】",
	"channel":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据canId和data获取信号

**

**请求URL**

/dbc/manage/findSignalByCanIdAndData `POST` 

**请求体**

```json
{
	"canId":"string【必须】",
	"data":"string【必须】",
	"channel":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据canId和data获取信号

**

**请求URL**

/dbc/manage/findSignalByCanIdAndDataList `POST` 

**请求体**

```json
{
	"signalDetails":[{
		"canId":"string",
		"data":"string",
		"channel":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
##  根据DbcNames删除dbc

**

**请求URL**

/dbc/manage/deleteByDbcNames `POST` 

**请求体**

```json
{
	"dbcNames":"string[]【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询dbc文件列表

**

**请求URL**

/dbc/manage/findAllDbcFile `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询dbc文件列表

**

**请求URL**

/dbc/manage/findDbcFileByCAN `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据DBCname查询

**

**请求URL**

/dbc/manage/findMessageByDbcName `POST` 

**请求体**

```json
{
	"dbcName":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据消息查询信号

**

**请求URL**

/dbc/manage/findSignalByMessage `POST` 

**请求体**

```json
{
	"dbcName":"string【必须】",
	"id":"int【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## dbc关联

**

**请求URL**

/dbc/manage/associateDbc `POST` 

**请求体**

```json
{
	"dbcFileAssociations":[{
		"name":"string //dbc 文件名称",
		"association":"int //是否关联 0是 1否"
	}],
	"selectedCAN":{
		"canName":"string",
		"canId":"string"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## dbc关联

**

**请求URL**

/dbc/manage/associateDbc `POST` 

**请求体**

```json
{
	"dbcFileAssociations":[{
		"name":"string //dbc 文件名称",
		"association":"int //是否关联 0是 1否"
	}],
	"selectedCAN":{
		"canName":"string",
		"canId":"string"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据DBCname查询-报文->信号树

**

**请求URL**

/dbc/manage/findSignalByDbcName `POST` 

**请求体**

```json
{
	"dbcName":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 诊断服务：诊断服务控制层
## 所有诊断服务列表查询

**

**请求URL**

/ecu/diagnosis/getAllDiagnosticServiceList `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 某诊断服务请求参数查询

**

**请求URL**

/ecu/diagnosis/getDiagnosticServiceParams `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"serviceId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 组装参数值

**

**请求URL**

/ecu/diagnosis/combineParams `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"addressMode":"string //寻址方式 1-物理寻址，2-功能寻址",
	"transmissionMode":"string //请求响应模式",
	"serviceId":"string //诊断服务id",
	"responseCheckValue":"string //响应校验值",
	"checkResponse":"string //检查响应 0，检查，1，不检查",
	"paramsList":[{
		"name":"string",
		"value":"int",
		"valueStr":"string",
		"vt":"string",
		"diagDataType":"string",
		"baseTypeEncoding":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 发送诊断服务请求接口

**

**请求URL**

/ecu/diagnosis/sendDiagnosticService `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"addressMode":"string //寻址方式 1-物理寻址，2-功能寻址",
	"transmissionMode":"string //请求响应模式",
	"serviceId":"string //诊断服务id",
	"responseCheckValue":"string //响应校验值",
	"checkResponse":"string //检查响应 0，检查，1，不检查",
	"paramsList":[{
		"name":"string",
		"value":"int",
		"valueStr":"string",
		"vt":"string",
		"diagDataType":"string",
		"baseTypeEncoding":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取解锁等级列表

**

**请求URL**

/ecu/diagnosis/getUnlockLevelList `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 快速解锁

**

**请求URL**

/ecu/diagnosis/quickUnlock `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"unlockId":"string //解锁请求id【必须】",
	"unlockLevel":"string //解锁请求unlockLevel【必须】",
	"addressMode":"string //寻址方式",
	"transmissionMode":"string //请求响应模式",
	"unlockLevelParam":"int //解锁请求unlockLevel【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 发送RawPdu数据

**

**请求URL**

/ecu/diagnosis/sendRawPdu `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"roundTime":"string //循环次数",
	"delayTime":"string //延时",
	"data":[{
		"seq":"int //序号",
		"sIdCode":"string //服务sid",
		"serviceId":"string",
		"requestData":"string //请求数据",
		"responseData":"string //响应数据",
		"addressingMode":"string //是否功能寻址",
		"checkResponse":"string //是否检查响应",
		"delay":"string //延时",
		"responsePolicy":[{
			"id":"long",
			"parentId":"long",
			"responsePolicyCode":"string",
			"relatedSeq":"string",
			"relatedUuid":"string"
		}],
		"suppressResponse":"string //是否抑制",
		"uuid":"string //uuid",
		"currentResponse":"object //currentResponse 批量发送响应结果",
		"custom":{
			"id":"long",
			"parentId":"long //父id",
			"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
			"type":"string //type security安全 upload上传 erasure擦除 check校验",
			"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
			"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
			"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
			"fileLength":"long //fileSize 文件大小 字节",
			"level":"int //27服务级别 1 3",
			"securityDllPath":"string //安全算法dll绝对路径"
		},
		"processId":"string //用来与processService关联使用",
		"type":"string"
	}],
	"channel":"string //频道"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 诊断服务响应DTC解析

**

**请求URL**

/ecu/diagnosis/dealDtcDid `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"tag":"string【必须】",
	"hexValue":"string【必须】",
	"requestSeed":"string //复制秘钥必传，判定解锁等级"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 场景诊断响应码流解析

**

**请求URL**

/ecu/diagnosis/displayDiagnosisDetails `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 修改诊断仪在线状态

**

**请求URL**

/ecu/diagnosis/updateDiagnosticLiveStatus `POST` 

**请求体**

```json
{
	"isSend":"int //是否需要发送诊断仪在线3E80 1-发送，0-不发送"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取诊断服务分类列表接口

**

**请求URL**

/ecu/diagnosis/getFunctionClassList `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"proId":"string //ECU部件协议id",
	"proKey":"string //ECU部件协议类型"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## ECU部件诊断服务列表查询

**

**请求URL**

/ecu/diagnosis/getDiagnosticServiceList `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"functionId":"string //诊断服务分类id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取诊断配置参数信息接口

**

**请求URL**

/ecu/diagnosis/getDiagnosticConfiguration `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 修改诊断配置参数信息接口

**

**请求URL**

/ecu/diagnosis/modifyDiagnosticConfiguration `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文导出接口

**

**请求URL**

/ecu/diagnosis/saveLogsMsg `GET` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 组装自定义参数值

**

**请求URL**

/ecu/diagnosis/combineCustomizeParams `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"tagId":"string【必须】",
	"seq":"int //序号",
	"sIdCode":"string //服务sid【必须】",
	"requestData":"string //请求数据",
	"responseData":"string //响应数据",
	"addressingMode":"string //是否功能寻址",
	"checkResponse":"string //是否检查响应",
	"delay":"string //延时",
	"responsePolicy":[{
		"id":"long",
		"parentId":"long",
		"responsePolicyCode":"string",
		"relatedSeq":"string",
		"relatedUuid":"string"
	}],
	"suppressResponse":"string //是否抑制",
	"uuid":"string //uuid",
	"currentResponse":{
		"responseData":"string //响应数据和响应校验",
		"responseResult":"string //响应结果，解释为可读的结果",
		"matchOrNot":"string //响应校验是否匹配",
		"responseParam":[{
			"id":"string",
			"name":"string",
			"chName":"string",
			"longName":"string //longName name",
			"description":"string //param 描述",
			"diagDataType":"string //param DIAG-CODED-TYPE 类型",
			"baseTypeEncoding":"string",
			"physicalDataType":"string //param PHYSICAL-TYPE  类型",
			"displayRadix":"string //param类型  DEC HEX ...",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			},
			"rawValue":"int",
			"hexValue":"string",
			"joinName":"string",
			"uiType":"string //固定0 下拉框1 区间2 输入框 3",
			"sectionList":"object //下拉框/区间   uiType = 1/2",
			"compuScalesList":"object //linear类型 compuScalesList",
			"byteMaxLength":"string //byte最大长度",
			"byteMinLength":"string //byte最小长度",
			"defaultValue":"string //根据DOP 进行选择",
			"object":"object //有可能存放 DOP-REF ,DOP-SNREF,TABLE-SNREF,TABLE-KEY-SNREF,(END-OF-PDU-FIELD--->STRUCTURE--->DOP)",
			"byteOrBit":"string //页面是按照byte类型还是bit类型处理",
			"bitLength":"int //bit 长度  小于",
			"relationId":"string",
			"keyHexValue":"string",
			"category":"string //texttable LINEAR IDENTICAL",
			"paramStringValue":"string //处理did/dtc/textTable 参数展示用",
			"bytePosition":"int",
			"bitPosition":"int",
			"myParamLinkedHashMap":{},
			"dtcObject":"object",
			"didObject":"object"
		}]
	},
	"custom":{
		"id":"long",
		"parentId":"long //父id",
		"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
		"type":"string //type security安全 upload上传 erasure擦除 check校验",
		"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
		"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
		"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
		"fileLength":"long //fileSize 文件大小 字节",
		"level":"int //27服务级别 1 3",
		"securityDllPath":"string //安全算法dll绝对路径"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 批量发送诊断服务请求

**

**请求URL**

/ecu/diagnosis/batchSendDiagnosticData `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"string",
	"seq":"int[]",
	"tagIdList":"long[]",
	"processesIdList":"long[]",
	"vehicleId":"string",
	"scenarioId":"string",
	"flowName":"string",
	"selectedEcuId":"string",
	"selectedEcuName":"string",
	"loopCount":"string",
	"path":"string",
	"level":"string",
	"handleType":"string //操作类型",
	"fileType":"string //文件类型",
	"sId":"string",
	"processId":"string",
	"interval":"string",
	"uuid":"string",
	"sigFilePath":"string",
	"logicAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 停止诊断服务

**

**请求URL**

/ecu/diagnosis/stopBatchSend `POST` 

**请求体**

```json
{
	"manfac":"string //车辆名称",
	"manfacId":"string //车辆ID",
	"ecuName":"string //部件名称",
	"ecuId":"string //部件ID"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询诊断数据列表接口

**

**请求URL**

/ecu/diagnosis/queryDiagDataList `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询诊断数据列表接口

**

**请求URL**

/ecu/diagnosis/queryDiagDataList `POST` 

**请求体**

```json
{
	"tagId":"string //数据"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 保存诊断数据列表当前数据的接口

**

**请求URL**

/ecu/diagnosis/saveDiagDataList `POST` 

**请求体**

```json
{
	"tagName":"string",
	"data":[{
		"seq":"int //序号",
		"sIdCode":"string //服务sid",
		"serviceId":"string",
		"requestData":"string //请求数据",
		"responseData":"string //响应数据",
		"addressingMode":"string //是否功能寻址",
		"checkResponse":"string //是否检查响应",
		"delay":"string //延时",
		"responsePolicy":[{
			"id":"long",
			"parentId":"long",
			"responsePolicyCode":"string",
			"relatedSeq":"string",
			"relatedUuid":"string"
		}],
		"suppressResponse":"string //是否抑制",
		"uuid":"string //uuid",
		"currentResponse":"object //currentResponse 批量发送响应结果",
		"custom":{
			"id":"long",
			"parentId":"long //父id",
			"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
			"type":"string //type security安全 upload上传 erasure擦除 check校验",
			"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
			"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
			"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
			"fileLength":"long //fileSize 文件大小 字节",
			"level":"int //27服务级别 1 3",
			"securityDllPath":"string //安全算法dll绝对路径"
		},
		"processId":"string //用来与processService关联使用",
		"type":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 保存诊断数据列表当前数据的接口

**

**请求URL**

/ecu/diagnosis/saveDiagDataList `POST` 

**请求体**

```json
{
	"tagName":"string",
	"tagId":"string",
	"dataList":[{
		"seq":"int //序号",
		"sIdCode":"string //服务sid",
		"serviceId":"string",
		"requestData":"string //请求数据",
		"responseData":"string //响应数据",
		"addressingMode":"string //是否功能寻址",
		"checkResponse":"string //是否检查响应",
		"delay":"string //延时",
		"responsePolicy":[{
			"id":"long",
			"parentId":"long",
			"responsePolicyCode":"string",
			"relatedSeq":"string",
			"relatedUuid":"string"
		}],
		"suppressResponse":"string //是否抑制",
		"uuid":"string //uuid",
		"currentResponse":"object //currentResponse 批量发送响应结果",
		"custom":{
			"id":"long",
			"parentId":"long //父id",
			"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
			"type":"string //type security安全 upload上传 erasure擦除 check校验",
			"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
			"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
			"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
			"fileLength":"long //fileSize 文件大小 字节",
			"level":"int //27服务级别 1 3",
			"securityDllPath":"string //安全算法dll绝对路径"
		},
		"processId":"string //用来与processService关联使用",
		"type":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 文件导入诊断数据列表

**

**请求URL**

/ecu/diagnosis/importDiagData `POST` 

**请求体**

```json
{
	"tags":[{
		"id":"long",
		"tagName":"string //tag名称"
	}],
	"path":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 批量导出诊断数据列表

**

**请求URL**

/ecu/diagnosis/exportDiagData `POST` 

**请求体**

```json
{
	"tags":[{
		"id":"long",
		"tagName":"string //tag名称"
	}],
	"path":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 标签重命名

**

**请求URL**

/ecu/diagnosis/tagRenameList `POST` 

**请求体**

```json
{
	"id":"long",
	"tagName":"string //tag名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 清空数据库接口

**

**请求URL**

/ecu/diagnosis/deleteAll `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 全局DID字段名称查询接口

**

**请求URL**

/ecu/diagnosis/querySelectedDidValues `POST` 

**请求体**

```json
{
	"manfac":"string //车型名称",
	"manfacId":"string //车型ID",
	"channel":"string //频道",
	"ip":"string //IP",
	"ecus":[{
		"didOrDtc":"string //判断DID这个EcuDto是DID还是DTC:\"DID\"和\"DTC\"",
		"ecuName":"string",
		"ecuId":"string",
		"manfac":"string //车型名称",
		"manfacId":"string //车型ID",
		"channel":"string //频道",
		"ip":"string //IP",
		"linkChannel":"string //信道",
		"baudRates":[{
			"value":"string",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			}
		}],
		"init":"int //初始化连接配置",
		"linkId":"int //连接ID",
		"logicAddress":{
			"testerLogicAddress":"int",
			"remoteLogicAddress":"int",
			"LinkType":"int //0doip 1can 2canfd"
		},
		"statusMask":"string //状态掩码",
		"dtcAndNumber":"byte[]",
		"dtcAndNumberMap":{},
		"dtcList":"string[]"
	}],
	"linkChannel":"string //信道",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 全局DID故障码查询接口

**

**请求URL**

/ecu/diagnosis/querySelectedDidFaultCodes `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"statusMask":"string",
	"channel":"string",
	"ip":"string",
	"ecus":[{
		"didOrDtc":"string //判断DID这个EcuDto是DID还是DTC:\"DID\"和\"DTC\"",
		"ecuName":"string",
		"ecuId":"string",
		"manfac":"string //车型名称",
		"manfacId":"string //车型ID",
		"channel":"string //频道",
		"ip":"string //IP",
		"linkChannel":"string //信道",
		"baudRates":[{
			"value":"string",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			}
		}],
		"init":"int //初始化连接配置",
		"linkId":"int //连接ID",
		"logicAddress":{
			"testerLogicAddress":"int",
			"remoteLogicAddress":"int",
			"LinkType":"int //0doip 1can 2canfd"
		},
		"statusMask":"string //状态掩码",
		"dtcAndNumber":"byte[]",
		"dtcAndNumberMap":{},
		"dtcList":"string[]"
	}],
	"linkChannel":"string //信道",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 指定车型和部门的清除全局DID故障码接口

**

**请求URL**

/ecu/diagnosis/cleanSelectedDidFaultCodes `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"statusMask":"string",
	"channel":"string",
	"ip":"string",
	"ecus":[{
		"didOrDtc":"string //判断DID这个EcuDto是DID还是DTC:\"DID\"和\"DTC\"",
		"ecuName":"string",
		"ecuId":"string",
		"manfac":"string //车型名称",
		"manfacId":"string //车型ID",
		"channel":"string //频道",
		"ip":"string //IP",
		"linkChannel":"string //信道",
		"baudRates":[{
			"value":"string",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			}
		}],
		"init":"int //初始化连接配置",
		"linkId":"int //连接ID",
		"logicAddress":{
			"testerLogicAddress":"int",
			"remoteLogicAddress":"int",
			"LinkType":"int //0doip 1can 2canfd"
		},
		"statusMask":"string //状态掩码",
		"dtcAndNumber":"byte[]",
		"dtcAndNumberMap":{},
		"dtcList":"string[]"
	}],
	"linkChannel":"string //信道",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询标签数据列表接口

**

**请求URL**

/ecu/diagnosis/queryTagDataList `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 新建标签数据接口

**

**请求URL**

/ecu/diagnosis/addTagData `POST` 

**请求体**

```json
{
	"tagName":"string",
	"tagId":"string",
	"dataList":[{
		"seq":"int //序号",
		"sIdCode":"string //服务sid",
		"serviceId":"string",
		"requestData":"string //请求数据",
		"responseData":"string //响应数据",
		"addressingMode":"string //是否功能寻址",
		"checkResponse":"string //是否检查响应",
		"delay":"string //延时",
		"responsePolicy":[{
			"id":"long",
			"parentId":"long",
			"responsePolicyCode":"string",
			"relatedSeq":"string",
			"relatedUuid":"string"
		}],
		"suppressResponse":"string //是否抑制",
		"uuid":"string //uuid",
		"currentResponse":"object //currentResponse 批量发送响应结果",
		"custom":{
			"id":"long",
			"parentId":"long //父id",
			"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
			"type":"string //type security安全 upload上传 erasure擦除 check校验",
			"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
			"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
			"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
			"fileLength":"long //fileSize 文件大小 字节",
			"level":"int //27服务级别 1 3",
			"securityDllPath":"string //安全算法dll绝对路径"
		},
		"processId":"string //用来与processService关联使用",
		"type":"string"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除标签数据接口

**

**请求URL**

/ecu/diagnosis/deleteTag `POST` 

**请求体**

```json
{
	"tagId":"string //数据"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 生成自定义刷写流程模板

**

**请求URL**

/ecu/diagnosis/addCustomTemplate `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 生成自定义刷写流程模板

**

**请求URL**

/ecu/diagnosis/getCarInfo `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 自定义数据保存前校验

**

**请求URL**

/ecu/diagnosis/checkCustom `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"custom":{
		"id":"long",
		"parentId":"long //父id",
		"relatedUuid":"string //relatedUuid 关联的DiagPolicyDTO的uuid",
		"type":"string //type security安全 upload上传 erasure擦除 check校验",
		"filePath":"string //filePath erasure擦除文件路径 upload上传文件绝对路径",
		"fileTypeId":"string //fileTypeId 文件类型id 北汽 长安 1驱动 2程序 3标定",
		"fileName":"string //fileFileName 文件名称，带后缀，不带路径",
		"fileLength":"long //fileSize 文件大小 字节",
		"level":"int //27服务级别 1 3",
		"securityDllPath":"string //安全算法dll绝对路径"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开始读取DTC

**

**请求URL**

/ecu/diagnosis/beginReadDTC `POST` 

**请求体**

```json
{
	"manfac":"string //车型名称【必须】",
	"manfacId":"string //车型ID【必须】",
	"statusMask":"string",
	"channel":"string",
	"ip":"string",
	"ecus":[{
		"didOrDtc":"string //判断DID这个EcuDto是DID还是DTC:\"DID\"和\"DTC\"",
		"ecuName":"string",
		"ecuId":"string",
		"manfac":"string //车型名称",
		"manfacId":"string //车型ID",
		"channel":"string //频道",
		"ip":"string //IP",
		"linkChannel":"string //信道",
		"baudRates":[{
			"value":"string",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			}
		}],
		"init":"int //初始化连接配置",
		"linkId":"int //连接ID",
		"logicAddress":{
			"testerLogicAddress":"int",
			"remoteLogicAddress":"int",
			"LinkType":"int //0doip 1can 2canfd"
		},
		"statusMask":"string //状态掩码",
		"dtcAndNumber":"byte[]",
		"dtcAndNumberMap":{},
		"dtcList":"string[]"
	}],
	"linkChannel":"string //信道",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 实时查询查询DTC信息

**

**请求URL**

/ecu/diagnosis/queryDTC `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 结束读取DTC信息

**

**请求URL**

/ecu/diagnosis/endReadDTC `POST` 

**请求体**

```json
{
	"endId":"int //endId 结束readDtc标识\r     \r      取值：0 不清除内存数据；1 清除内存数据【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开始读取DID

**

**请求URL**

/ecu/diagnosis/beginReadDID `POST` 

**请求体**

```json
{
	"manfac":"string //车型名称【必须】",
	"manfacId":"string //车型ID【必须】",
	"channel":"string //频道【必须】",
	"ip":"string //IP",
	"ecus":[{
		"didOrDtc":"string //判断DID这个EcuDto是DID还是DTC:\"DID\"和\"DTC\"",
		"ecuName":"string",
		"ecuId":"string",
		"manfac":"string //车型名称",
		"manfacId":"string //车型ID",
		"channel":"string //频道",
		"ip":"string //IP",
		"linkChannel":"string //信道",
		"baudRates":[{
			"value":"string",
			"unit":{
				"name":"string //单位 shortName",
				"longName":"string //单位longName",
				"displayName":"string //单位符号缩写 s ms m/s ...",
				"factorSiToUnit":"string //单位换算",
				"offsetSiToUnit":"string"
			}
		}],
		"init":"int //初始化连接配置",
		"linkId":"int //连接ID",
		"logicAddress":{
			"testerLogicAddress":"int",
			"remoteLogicAddress":"int",
			"LinkType":"int //0doip 1can 2canfd"
		},
		"statusMask":"string //状态掩码",
		"dtcAndNumber":"byte[]",
		"dtcAndNumberMap":{},
		"dtcList":"string[]"
	}],
	"linkChannel":"string //信道",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 实时查询查询DID信息

**

**请求URL**

/ecu/diagnosis/queryDID `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 结束读取DID信息

**

**请求URL**

/ecu/diagnosis/endReadDID `POST` 

**请求体**

```json
{
	"endId":"int //endId 结束readDid标识\r     \r      取值：0 不清除内存数据；1 清除内存数据【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 添加bat脚本

**

**请求URL**

/ecu/diagnosis/addBat `POST` 

**请求体**

```json
{
	"path":"string //bat脚本地址",
	"tagId":"long",
	"seq":"int",
	"uuid":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 服务编排：Dvp 控制层
## DVP 发送

*作者: zWX929034*

**请求URL**

/dvp/manage/send `POST` 

**请求体**

```json
{
	"data":[{
		"uuid":"string",
		"title":"string",
		"service":"string",
		"status":"string",
		"actionParam":[{
			"enParam":"string",
			"chParam":"string",
			"value":"int"
		}]
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## DVP  查询

*作者: zWX929034*

**请求URL**

/dvp/manage/findDvpById `POST` 

**请求体**

```json
{
	"uuid":"string[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：Ecu 控制层
## 校验后台启动是否成功

*作者: ZWX1083624*

**请求URL**

/vehicleEcu/getBackStatus `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取车型信息列表

*作者: ZWX1083624*

**请求URL**

/vehicleEcu/getVehicleList `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取指定车型的详情信息列表

*作者: ZWX1083624*

**请求URL**

/vehicleEcu/getVehicleDetail `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 变更部件的在线状态

*作者: ZWX1083624*

**请求URL**

/vehicleEcu/changeEcuPhysicalOnline `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"ecuIdList":"string[]",
	"isPhysicalOnline":"int //部件物理在线状态 0-不在线，1-在线【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型切换

*作者: ZWX1083624*

**请求URL**

/vehicleEcu/switch `POST` 

**请求体**

```json
{
	"vehicleId":"string //需要切换车型的ID【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 快速诊断：故障码和did导出
## 导出

**

**请求URL**

/ecu/export/exportDIDToExcel `GET` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
json|string|否|json

**返回结果**

```json
{}
```
## 导出故障码

**

**请求URL**

/ecu/export/exportDidFaultCodesToExcel `GET` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
json|string|否|json

**返回结果**

```json
{}
```
# 配置：FileSelect控制层 获取文件
## 查询电脑路径信息

*作者: zWX929034*

**请求URL**

/file/find/list `POST` 

**请求体**

```json
{
	"currentPath":"string //currentPath  当前文件夹路径",
	"filetype":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 日志采集
## 日志采集 开始MCU告警记录读取

**

**请求URL**

/logCollection/custom/beginMcuAlarmCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 开始MCU告警记录结果查询

**

**请求URL**

/logCollection/custom/queryMcuAlarmCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 开始MCU告警记录读取

**

**请求URL**

/logCollection/custom/stopMcuAlarmCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 开始MCU数据仪记录读取

**

**请求URL**

/logCollection/custom/beginMcuDataLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 MCU数据仪记录读取

**

**请求URL**

/logCollection/custom/queryMcuDataLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 停止MCU数据仪记录读取

**

**请求URL**

/logCollection/custom/stopMcuDataLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开始PDU日志采集

**

**请求URL**

/logCollection/custom/beginPduLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"logType":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导出PDU日志采集

**

**请求URL**

/logCollection/custom/exportPduLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"logType":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 清除日志采集数据

**

**请求URL**

/logCollection/custom/clearPduLogCollection `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 开始GW日志采集

**

**请求URL**

/logCollection/custom/beginGwLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通道，Ethernet CAN【必须】",
	"logType":"string //日志类型，dlt，det，flash，trap【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 GW日志采集结果查询

**

**请求URL**

/logCollection/custom/queryGwLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通道，Ethernet CAN【必须】",
	"logType":"string //日志类型，dlt，det，flash，trap【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 停止GW日志采集

**

**请求URL**

/logCollection/custom/stopGwLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通道，Ethernet CAN【必须】",
	"logType":"string //日志类型，dlt，det，flash，trap【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 清除GW日志采集

**

**请求URL**

/logCollection/custom/clearGwLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通道，Ethernet CAN【必须】",
	"logType":"string //日志类型，dlt，det，flash，trap【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志采集 导出GW日志采集

**

**请求URL**

/logCollection/custom/exportGwLogCollection `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通道，Ethernet CAN【必须】",
	"logType":"string //日志类型，dlt，det，flash，trap【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 日志采集：日志处理控制层
## 检查日志文件接口

*作者: zwx928923*

**请求URL**

/log/checkLogFile `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 打开日志目录接口

*作者: zwx928923*

**请求URL**

/log/openLogDir `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：Lucene控制类
## findByGroup接口

**

**请求URL**

/lucene/findByGroup `GET` `POST` 


**返回结果**

```json
Map{}
```
# 诊断服务Controller
## 报文发送立即发送

**

**请求URL**

/send/sendMessage `POST` 

**请求体**

```json
{
	"ecuName":"string",
	"sendMessage":{
		"channel":"string【必须】",
		"messageType":"string //帧类型 默认 标准帧 0为标准帧，后续可添加【必须】",
		"messageFormat":"string //帧格式 默认 数据帧 0为数据帧，后续可添加【必须】",
		"messageId":"string //帧id  can/canFd协议需要",
		"dataLength":"string //数据长度【必须】",
		"dataBytes":"string //数据【必须】",
		"sendCount":"string //发送次数 可为0，但不可为空【必须】",
		"sendDelay":"string //每次间隔 可为0，但不可为空 单位为ms【必须】",
		"messageCount":"string //每次发送次数 可为0，但不可为空【必须】",
		"messageName":"string //名称（可选）",
		"isIdAdd":"string //ID递增 can/canFd协议需要 //1勾选，0未勾选",
		"isDataAdd":"string //数据递增 可为0，但不可为空 //1勾选，0未勾选【必须】",
		"dataAdd":{
			"startByte":"int",
			"addByteCount":"int"
		}
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/queryMessageList `POST` 

**请求体**

```json
{
	"canList":[{
		"canName":"string",
		"canId":"string"
	}],
	"dt":"string //dt  时间是否显示为△t  \"true\"",
	"channel":"string",
	"directions":"string[] //directions方向的集合，Rx、Ry等，用于筛选功能",
	"messageIds":"string[] //messageId帧ID的集合，用于筛选功能",
	"messageTypes":"string[] //messageType帧类型的集合，用于筛选功能",
	"dlcs":"string[] //dlc的集合，用于筛选功能",
	"pageNumber":"int //第N页的页数，用于分页",
	"pageSize":"int //每一页的尺寸，用于分页",
	"messageNames":"string[] //messageName帧名称的集合，用于筛选功能",
	"sort":"string //排序 {排序的字段 : 排序方向（DESC降序，ASC升序）}",
	"tableName":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/emptyMessage `POST` 

**请求体**

```json
{
	"channel":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/batchSendMessage `POST` 

**请求体**

```json
{
	"sendModel":"string",
	"messageList":[{
		"channel":"string【必须】",
		"messageType":"string //帧类型 默认 标准帧 0为标准帧，后续可添加【必须】",
		"messageFormat":"string //帧格式 默认 数据帧 0为数据帧，后续可添加【必须】",
		"messageId":"string //帧id  can/canFd协议需要",
		"dataLength":"string //数据长度【必须】",
		"dataBytes":"string //数据【必须】",
		"sendCount":"string //发送次数 可为0，但不可为空【必须】",
		"sendDelay":"string //每次间隔 可为0，但不可为空 单位为ms【必须】",
		"messageCount":"string //每次发送次数 可为0，但不可为空【必须】",
		"messageName":"string //名称（可选）",
		"isIdAdd":"string //ID递增 can/canFd协议需要 //1勾选，0未勾选",
		"isDataAdd":"string //数据递增 可为0，但不可为空 //1勾选，0未勾选【必须】",
		"dataAdd":{
			"startByte":"int",
			"addByteCount":"int"
		}
	}],
	"ecuName":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/stopSendMessage `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/saveToAsc `GET` 


**返回结果**

```json
{}
```
## 报文发送查询数据接口

**

**请求URL**

/send/queryUpdateProgress `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/setMaxCount `POST` 

**请求体**

```json
{
	"saveLimit":"long"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 报文发送查询数据接口

**

**请求URL**

/send/queryMaxCount `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 报文镜像：报文处理控制类
## 报文导出接口

*作者: zwx928923*

**请求URL**

/msg/exportMsg `GET` 


**返回结果**

```json
{}
```
## clearMsg

*作者: zwx928923*

**请求URL**

/msg/clearMsg `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 我的下载 Controller
## 我的下载信息新增接口

*作者: qwx1180239*

**请求URL**

/version/saveMyDownloadInfo `POST` 

**请求体**

```json
{
	"pageNum":"int",
	"pageSize":"int",
	"size":"int",
	"startRow":"int",
	"endRow":"int",
	"pages":"int",
	"prePage":"int",
	"nextPage":"int",
	"isFirstPage":"boolean",
	"isLastPage":"boolean",
	"hasPreviousPage":"boolean",
	"hasNextPage":"boolean",
	"navigatePages":"int",
	"navigatepageNums":"int[]",
	"navigateFirstPage":"int",
	"navigateLastPage":"int",
	"total":"long",
	"list":"[]",
	"id":"long //自增id",
	"name":"string //文件名称",
	"downLoadTime":"string //下载时间 今天 ”1“，一周 \"2\", 一个月 \"3\", 三个月 \"4\" ,自定义时间 \"5\" ,全部 ”6“",
	"downloadStatus":"string //下载状态  枚举值：0：下载中；1：已完成；2：下载失败；3、已删除",
	"path":"string //本地下载路径:本地完成路径",
	"downloadUrl":"string //远程下载路径",
	"srcFileDownloadUrl":"string //源文件下载地址",
	"srcFileSize":"string //源文件大小",
	"sigFileDownloadUrl":"string //签名文件下载地址",
	"sigFileSize":"long //签名文件大小",
	"sigStatus":"string //验签结果 1:成功 , 2:失败 0:验签中",
	"realFileName":"string //文件真实名称-存储本地文件名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 我的下载信息删除接口

*作者: qwx1180239*

**请求URL**

/version/deleteMyDownloadInfo `POST` 

**请求体**

```json
{
	"pageNum":"int",
	"pageSize":"int",
	"size":"int",
	"startRow":"int",
	"endRow":"int",
	"pages":"int",
	"prePage":"int",
	"nextPage":"int",
	"isFirstPage":"boolean",
	"isLastPage":"boolean",
	"hasPreviousPage":"boolean",
	"hasNextPage":"boolean",
	"navigatePages":"int",
	"navigatepageNums":"int[]",
	"navigateFirstPage":"int",
	"navigateLastPage":"int",
	"total":"long",
	"list":"[]",
	"id":"long //自增id",
	"name":"string //文件名称",
	"downLoadTime":"string //下载时间 今天 ”1“，一周 \"2\", 一个月 \"3\", 三个月 \"4\" ,自定义时间 \"5\" ,全部 ”6“",
	"downloadStatus":"string //下载状态  枚举值：0：下载中；1：已完成；2：下载失败；3、已删除",
	"path":"string //本地下载路径:本地完成路径",
	"downloadUrl":"string //远程下载路径",
	"srcFileDownloadUrl":"string //源文件下载地址",
	"srcFileSize":"string //源文件大小",
	"sigFileDownloadUrl":"string //签名文件下载地址",
	"sigFileSize":"long //签名文件大小",
	"sigStatus":"string //验签结果 1:成功 , 2:失败 0:验签中",
	"realFileName":"string //文件真实名称-存储本地文件名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 我的下载信息修改接口

*作者: qwx1180239*

**请求URL**

/version/updateMyDownloadInfo `POST` 

**请求体**

```json
{
	"pageNum":"int",
	"pageSize":"int",
	"size":"int",
	"startRow":"int",
	"endRow":"int",
	"pages":"int",
	"prePage":"int",
	"nextPage":"int",
	"isFirstPage":"boolean",
	"isLastPage":"boolean",
	"hasPreviousPage":"boolean",
	"hasNextPage":"boolean",
	"navigatePages":"int",
	"navigatepageNums":"int[]",
	"navigateFirstPage":"int",
	"navigateLastPage":"int",
	"total":"long",
	"list":"[]",
	"id":"long //自增id",
	"name":"string //文件名称",
	"downLoadTime":"string //下载时间 今天 ”1“，一周 \"2\", 一个月 \"3\", 三个月 \"4\" ,自定义时间 \"5\" ,全部 ”6“",
	"downloadStatus":"string //下载状态  枚举值：0：下载中；1：已完成；2：下载失败；3、已删除",
	"path":"string //本地下载路径:本地完成路径",
	"downloadUrl":"string //远程下载路径",
	"srcFileDownloadUrl":"string //源文件下载地址",
	"srcFileSize":"string //源文件大小",
	"sigFileDownloadUrl":"string //签名文件下载地址",
	"sigFileSize":"long //签名文件大小",
	"sigStatus":"string //验签结果 1:成功 , 2:失败 0:验签中",
	"realFileName":"string //文件真实名称-存储本地文件名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 我的下载信息查询接口

*作者: qwx1180239*

**请求URL**

/version/queryMyDownloadInfo `POST` 

**请求体**

```json
{
	"pageNum":"int",
	"pageSize":"int",
	"size":"int",
	"startRow":"int",
	"endRow":"int",
	"pages":"int",
	"prePage":"int",
	"nextPage":"int",
	"isFirstPage":"boolean",
	"isLastPage":"boolean",
	"hasPreviousPage":"boolean",
	"hasNextPage":"boolean",
	"navigatePages":"int",
	"navigatepageNums":"int[]",
	"navigateFirstPage":"int",
	"navigateLastPage":"int",
	"total":"long",
	"list":"[]",
	"id":"long //自增id",
	"name":"string //文件名称",
	"downLoadTime":"string //下载时间 今天 ”1“，一周 \"2\", 一个月 \"3\", 三个月 \"4\" ,自定义时间 \"5\" ,全部 ”6“",
	"downloadStatus":"string //下载状态  枚举值：0：下载中；1：已完成；2：下载失败；3、已删除",
	"path":"string //本地下载路径:本地完成路径",
	"downloadUrl":"string //远程下载路径",
	"srcFileDownloadUrl":"string //源文件下载地址",
	"srcFileSize":"string //源文件大小",
	"sigFileDownloadUrl":"string //签名文件下载地址",
	"sigFileSize":"long //签名文件大小",
	"sigStatus":"string //验签结果 1:成功 , 2:失败 0:验签中",
	"realFileName":"string //文件真实名称-存储本地文件名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# OauthController类
## 获取 AccessCode

**

**请求URL**

/accessCode `GET` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过 AccessCode 获取 AccessToken

**

**请求URL**

/callBack `GET` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通过 AccessToken 获取 UserInfo

**

**请求URL**

/getUserInfo `GET` `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 接口1

**

**请求URL**

/{ /index } `GET` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 接口2

**

**请求URL**

/logout `GET` `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 报文镜像：以太镜像配置
## 查询L2以太镜像配置页面数据

**

**请求URL**

/mirroring/queryL2EthBusMirrorData `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取L2以太镜像配置

**

**请求URL**

/mirroring/getL2EthBusMirrorConf `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNoListL2":"string[]",
	"idListL2":"long[]",
	"dataNoListL3":"string[]",
	"idListL3":"long[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除L2以太镜像配置

**

**请求URL**

/mirroring/delL2EthBusMirrorConf `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNoList":"string[]",
	"idList":"long[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## L2以太镜像配置 新增

**

**请求URL**

/mirroring/additionsL2EthBusMirrorData `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNo":"string",
	"action":"string",
	"protocalType":"string",
	"d1Prio":"string",
	"srcMacAddr":"string",
	"dstMacAddr":"string",
	"vlanId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询L3以太镜像配置页面数据

**

**请求URL**

/mirroring/queryL3EthBusMirrorData `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## L3以太镜像配置 新增

**

**请求URL**

/mirroring/additionsL3EthBusMirrorData `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNo":"string",
	"action":"string",
	"protocalType":"string",
	"msgType":"string",
	"msgCode":"string",
	"tos":"string",
	"srcIPV4Addr":"string",
	"dstIPV4Addr":"string",
	"srcPort":"string",
	"dstPort":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取L3以太镜像配置

**

**请求URL**

/mirroring/getL3EthBusMirrorConf `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除L3以太镜像配置

**

**请求URL**

/mirroring/delL3EthBusMirrorConf `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNoList":"string[]",
	"idList":"long[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开启镜像

**

**请求URL**

/mirroring/enableEthBusMirror `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"l2EthBusMirrorList":[{
		"dataNo":"string",
		"action":"string",
		"protocalType":"string",
		"d1Prio":"string",
		"srcMacAddr":"string",
		"dstMacAddr":"string",
		"vlanId":"string",
		"effective":"string"
	}],
	"l3EthBusMirrorList":[{
		"dataNo":"string",
		"action":"string",
		"protocalType":"string",
		"iCMPMsgType":"string",
		"iCMPMsgCode":"string",
		"tOS":"string",
		"srcIPV4Addr":"string",
		"dstIPV4Addr":"string",
		"srcPort":"string",
		"dstPort":"string",
		"effective":"string"
	}],
	"start":"string",
	"numbersFigure":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭镜像

**

**请求URL**

/mirroring/disableEthBusMirror `POST` 

**请求体**

```json
{
	"manfac":"string",
	"manfacId":"string",
	"ecuName":"string",
	"ecuId":"string",
	"dataNoListL2":"string[]",
	"idListL2":"long[]",
	"dataNoListL3":"string[]",
	"idListL3":"long[]"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 保存xml

**

**请求URL**

/mirroring/saveChanges `POST` 

**请求体**

```json
{
	"xmlNumber":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 报文镜像：PacketTracing控制层
## 查询BusMirroring列表

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringList `POST` 

**请求体**

```json
{
	"canList":[{
		"canName":"string",
		"canId":"string"
	}],
	"stop":"string //stop  1:暂停，0：继续",
	"messageIds":"string[] //messageId帧ID的集合，用于筛选功能",
	"pageNumber":"int //第N页的页数，用于分页",
	"pageSize":"int //每一页的尺寸，用于分页"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 暂停时查询BusMirroring历史数据列表

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringListPause `POST` 

**请求体**

```json
{
	"canList":[{
		"canName":"string",
		"canId":"string"
	}],
	"stop":"string //stop  1:暂停，0：继续",
	"messageIds":"string[] //messageId帧ID的集合，用于筛选功能",
	"pageNumber":"int //第N页的页数，用于分页",
	"pageSize":"int //每一页的尺寸，用于分页"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 暂停查询BusMirroring列表预加载

*作者: zWX929034*

**请求URL**

/auto/config/stopPreLoad `POST` 

**请求体**

```json
{
	"status":"int //0-非暂停，1-暂停"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询BusMirroring列表canId分组

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirrorings `POST` 

**请求体**

```json
{
	"canList":[{
		"canName":"string",
		"canId":"string"
	}],
	"stop":"string //stop  1:暂停，0：继续",
	"messageIds":"string[] //messageId帧ID的集合，用于筛选功能",
	"pageNumber":"int //第N页的页数，用于分页",
	"pageSize":"int //每一页的尺寸，用于分页"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询BusMirroring列表(历史回放)

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringListPlayBack `POST` 

**请求体**

```json
{
	"canList":[{
		"canName":"string",
		"canId":"string"
	}],
	"dt":"string //dt  时间是否显示为△t  \"true\"",
	"stop":"string //stop  1:暂停，0：继续",
	"messageIds":"string[] //messageId帧ID的集合，用于筛选功能",
	"messageNames":"string[] //messageName帧名称的集合，用于筛选功能",
	"messageTypes":"string[] //messageType帧类型的集合，用于筛选功能",
	"directions":"string[] //directions方向的集合，Rx、Ry等，用于筛选功能",
	"dlcs":"string[] //dlc的集合，用于筛选功能",
	"pageNumber":"int //第N页的页数，用于分页",
	"pageSize":"int //每一页的尺寸，用于分页",
	"sort":"string //排序 {排序的字段 : 排序方向（DESC降序，ASC升序）}",
	"tableName":"string",
	"tableId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据ID删除BusMirroring记录

*作者: zWX929034*

**请求URL**

/auto/config/deleteBusMirroringById `POST` 

**请求体**

```json
{
	"busMirroringId":"string",
	"busMirroringIndex":"long"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询单个BusMirroring记录的信号值

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringSignals `POST` 

**请求体**

```json
{
	"messageId":"string",
	"hexStr":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## BusMirroring报文跟踪页面设置

*作者: zWX929034*

**请求URL**

/auto/config/busMirroringSetting `POST` 

**请求体**

```json
{
	"busMirrorSaveLimit":"long //页面显示最大条数",
	"timeShowFormat":"string //报文跟踪页面时间显示格式, 0 是 s.SSS ,1 是 yyyy-MM-dd hh:mi:ss.SSS"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## BusMirroring报文跟踪页面设置数据查询

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringSetting `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 设置Lucene数据库保存的BusMirroring记录最大数

*作者: zWX929034*

**请求URL**

/auto/config/setBusMirroringSaveLimit `POST` 

**请求体**

```json
{
	"busMirrorSaveLimit":"long"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询BusMirroring当前设置的保存的最大值

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringSaveLimit `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 设置报文跟踪显示的时间的格式

*作者: zWX929034*

**请求URL**

/auto/config/setBusMirroringTimeFormat `POST` 

**请求体**

```json
{
	"timeShowFormat":"string //报文跟踪页面时间显示格式, 0 是 s.SSS ,1 是 yyyy-MM-dd hh:mi:ss.SSS"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询报文跟踪显示的时间的格式

*作者: zWX929034*

**请求URL**

/auto/config/queryBusMirroringTimeFormat `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 将当前lucene中的数据保存到asc文件中

*作者: zWX929034*

**请求URL**

/auto/config/saveAsc `GET` 


**返回结果**

```json
{}
```
## 查询所有的帧ID

*作者: zWX929034*

**请求URL**

/auto/config/queryAllCanIds `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询所有的帧ID

*作者: zWX929034*

**请求URL**

/auto/config/queryAllCanIdsPlayBack `POST` 

**请求体**

```json
{
	"tableId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询所有的帧ID

*作者: zWX929034*

**请求URL**

/auto/config/queryAllCanChannelPlayBack `POST` 

**请求体**

```json
{
	"tableId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 清空busMirroring数据

*作者: zWX929034*

**请求URL**

/auto/config/clearBusMirroring `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 批量文件导入asc文件

*作者: zWX929034*

**请求URL**

/auto/config/importAsc `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file[]|否|multipartFiles

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导入DBC文件

*作者: zWX929034*

**请求URL**

/auto/config/importAsc `POST` 

**请求体**

```json
{
	"filePath":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭Asc的tab页

*作者: zWX929034*

**请求URL**

/auto/config/closeAscTab `POST` 

**请求体**

```json
{
	"tableName":"string //关闭的asc的tab页的名字同时也是表名",
	"tableId":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭Asc的所有tab页

*作者: zWX929034*

**请求URL**

/auto/config/closeAllTab `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：参数控制器
## ECU部件车型信息查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getCarType `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## ECU部件车型信息查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/findCarType `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件配置信息修改接口

*作者: zhangzhaojing*

**请求URL**

/ecu/modifyEcuConfig `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecu":[{
		"ecuName":"string【必须】",
		"ecuId":"string【必须】"
	}],
	"isPhysicOnline":"string //组件是否物理在线 0是1否，关键字段，不能为null，而且调用trim()后，长度必须大于0【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车辆初始化接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getEcuInitStatus `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id",
	"ip":"string //ECU部件ip",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string",
	"mode":"int //整车模式下 全局诊断连接 为1"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 打开帮助浏览器接口

*作者: zhangzhaojing*

**请求URL**

/ecu/openExplorer `POST` 

**请求体**

```json
{
	"url":"string //浏览器网址【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 建立连接接口  1.13.xB002 版本

*作者: zhangzhaojing*

**请求URL**

/ecu/connectEcu `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string //通道号 can/canfd 时有值  M 0 [0-15]",
	"ip":"string //ECU部件ip",
	"isAccessFlag":"string //部件连接接入认证标识，0-长安、北汽GW TLS认证 1-长安 AIS部件接入认证 2-不需要接入认证",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭链路  1.8.x版本

*作者: zhangzhaojing*

**请求URL**

/ecu/closeEcuLine `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 版本号查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/accessStop `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 版本号查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getAccessStatus `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 版本号查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getCarVersion `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 文件类型列表查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getFileTypeList `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture",
	"manfacId":"string //汽车制造商id",
	"ecuName":"string //ECU部件名称",
	"ecuId":"string //ECU部件id"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开始刷写接口

*作者: zwx928923*

**请求URL**

/ecu/beginUpdate `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //频道",
	"flashDir":"string //刷写文件目录【必须】",
	"isChangeAB":"string",
	"isBoot":"string",
	"flashFileList":[{
		"fileName":"string //文件名【必须】",
		"fileTypeId":"string //刷写文件类型，取值参照FileTypeEnum中的id【必须】",
		"length":"long //文件大小，单位byte",
		"lastUpdateTime":"string //文件最后修改时间，格式： yyyy-MM-dd HH:mm:ss【必须】"
	}],
	"remoteLogicAddress":"string //ECU部件远程逻辑地址【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 开始刷写接口

*作者: zwx928923*

**请求URL**

/ecu/beginUpdateBoot `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //频道",
	"flashDir":"string //刷写文件目录【必须】",
	"isChangeAB":"string",
	"isBoot":"string",
	"flashFileList":[{
		"fileName":"string //文件名【必须】",
		"fileTypeId":"string //刷写文件类型，取值参照FileTypeEnum中的id【必须】",
		"length":"long //文件大小，单位byte",
		"lastUpdateTime":"string //文件最后修改时间，格式： yyyy-MM-dd HH:mm:ss【必须】"
	}],
	"remoteLogicAddress":"string //ECU部件远程逻辑地址【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 结束刷写接口

*作者: zhangzhaojing*

**请求URL**

/ecu/stopUpdate `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"remoteLogicAddress":"string //ECU部件远程逻辑地址"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## AB区切换接口

*作者: zhangzhaojing*

**请求URL**

/ecu/changeAB `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string //通道号 can/canfd 时有值  M 0 [0-15]",
	"ip":"string //ECU部件ip",
	"isAccessFlag":"string //部件连接接入认证标识，0-长安、北汽GW TLS认证 1-长安 AIS部件接入认证 2-不需要接入认证",
	"loopCount":"string //循环次数",
	"ecuLoopCount":"string //循环次数",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"flowIds":"long[]",
	"isContinue":"string //场景测试 1：错停 :0：错顺"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## AB区回滚接口

*作者: zhangzhaojing*

**请求URL**

/ecu/rollbackAB `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"channel":"string //通讯频道【必须】",
	"linkChannel":"string //通道号 can/canfd 时有值  M 0 [0-15]",
	"ip":"string //ECU部件ip",
	"isAccessFlag":"string //部件连接接入认证标识，0-长安、北汽GW TLS认证 1-长安 AIS部件接入认证 2-不需要接入认证",
	"loopCount":"string //循环次数",
	"ecuLoopCount":"string //循环次数",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"flowIds":"long[]",
	"isContinue":"string //场景测试 1：错停 :0：错顺"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志查询接口

*作者: zhangzhaojing*

**请求URL**

/ecu/queryLog `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture",
	"manfacId":"string //汽车制造商id",
	"ecuName":"string //ECU部件名称",
	"ecuId":"string //ECU部件id",
	"logType":"int //首页连接日志:1, 刷写页面操作日志:2, 报文日志:3, 执行步骤日志:4",
	"startId":"long //日志开始id",
	"date":"date //查询起始时间"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 日志导出接口
导出报文监控和执行步骤

*作者: zhangzhaojing*

**请求URL**

/ecu/exportReport `GET` 


**返回结果**

```json
{}
```
## 单个文件上传接口

*作者: zhangzhaojing*

**请求URL**

/ecu/uploadFile `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|前端传输文件

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 单个文件上传接口

*作者: zhangzhaojing*

**请求URL**

/ecu/uploadFile `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
file|file|否|multipartFile前端传输文件
manfac|string|是|汽车制造商 manufacture
manfacId|string|是|汽车制造商id
ecuName|string|是|ECU部件名称
ecuId|string|是|ECU部件id
fileChunkName|string|否|切片文件名 abc.bin-1
fileName|string|否|切片前文件名 abc.bin
fileUId|string|否|文件唯一标识
fileOrder|string|否|文件顺序
fileSize|long|否|切片前文件大小 单位字节
chunkNumber|long|否|分片个数
fileType|string|否|文件类型 1、2、3、4、5
uploadId|string|否|uploadId上传目录

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 关闭刷写页签回调接口

*作者: zhangzhaojing*

**请求URL**

/ecu/closeUpdateTab `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取上传uploadId接口

*作者: zhangzhaojing*

**请求URL**

/ecu/getUploadId `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 功能描述

*作者: yWX970191*

**请求URL**

/ecu/queryLogLevel `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 功能描述

*作者: yWX970191*

**请求URL**

/ecu/editLogLevel `POST` 

**请求体**

```json
{
	"logLevel":"string //日志等级 info debugger",
	"isIgnoreOtherFun":"int //是否忽略功能寻址 0是 1否",
	"isDevelopmentMode":"int"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 功能描述 设置上位机网卡ip

*作者: zhangzhaojing*

**请求URL**

/ecu/setLocalIp `POST` 

**请求体**

```json
{
	"ipName":"string",
	"localIp":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 功能描述 获取历史刷写文件路径

*作者: zwx928923*

**请求URL**

/ecu/searchHisFlashDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 读取刷写文件目录

*作者: zwx928923*

**请求URL**

/ecu/readFlashDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"flashDir":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 读取刷写文件目录

*作者: zwx928923*

**请求URL**

/ecu/readFlashDirCur `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"flashDir":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 校验刷写文件

*作者: zwx928923*

**请求URL**

/ecu/checkFlashFile `POST` 

**请求体**

```json
{
	"flashDir":"string【必须】",
	"flashFileList":[{
		"fileName":"string //文件名【必须】",
		"fileTypeId":"string //刷写文件类型，取值参照FileTypeEnum中的id【必须】",
		"length":"long //文件大小，单位byte",
		"lastUpdateTime":"string //文件最后修改时间，格式： yyyy-MM-dd HH:mm:ss【必须】"
	}]
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除刷写目录

*作者: zwx928923*

**请求URL**

/ecu/deleteFlashDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"id":"long【必须】",
	"flashDir":"string //文件路径"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 添加刷写目录

*作者: zwx928923*

**请求URL**

/ecu/addFlashDir `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture【必须】",
	"manfacId":"string //汽车制造商id【必须】",
	"ecuName":"string //ECU部件名称【必须】",
	"ecuId":"string //ECU部件id【必须】",
	"flashDir":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询当前网卡ip列表接口

*作者: zhangzhaojing*

**请求URL**

/ecu/queryIpList `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件清除秘钥学习

*作者: zhangzhaojing*

**请求URL**

/ecu/clearEcuKey `POST` 

**请求体**

```json
{
	"manfac":"string //汽车制造商 manufacture",
	"manfacId":"string //汽车制造商id",
	"ecuName":"string //ECU部件名称",
	"ecuId":"string //ECU部件id",
	"ip":"string //ECU部件ip",
	"baudRates":[{
		"value":"string",
		"unit":{
			"name":"string //单位 shortName",
			"longName":"string //单位longName",
			"displayName":"string //单位符号缩写 s ms m/s ...",
			"factorSiToUnit":"string //单位换算",
			"offsetSiToUnit":"string"
		}
	}],
	"channel":"string //通讯频道",
	"linkChannel":"string",
	"status":"int //链路状态 1-连接，其他-未连接"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件-ECU密钥信息查询

*作者: zhangzhaojing*

**请求URL**

/ecu/queryEcuKey `POST` 

**请求体**

```json
{
	"ecuId":"string //ECU部件id",
	"ecuName":"string //ECU部件名称"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件-修改ECU密钥

*作者: zhangzhaojing*

**请求URL**

/ecu/updateEcuKey `POST` 

**请求体**

```json
{
	"ecuId":"string //ECU部件id",
	"ecuName":"string //ECU部件名称",
	"ecuKey":"string //秘玥"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询ECU密钥历史记录

*作者: zhangzhaojing*

**请求URL**

/ecu/selectEcuKeysHistory `POST` 

**请求体**

```json
{
	"ecuId":"string //ECU部件id【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 刷写：密钥灌装控制层
## 密钥灌装

**

**请求URL**

/secretKeyFilling/startFilling `POST` 

**请求体**

```json
[{
	"logicalAddress":"string //@NotNull",
	"key":"string[]"
}]
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 报文镜像：uds信号实时分析
## addBatch

*作者: yWX970191*

**请求URL**

/analysisOnLine/addBatch `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 新增信号列表数据

*作者: yWX970191*

**请求URL**

/analysisOnLine/addBatch `POST` 

**请求体**

```json
{
	"list":[{
		"id":"int",
		"dbcFileName":"string",
		"signalName":"string"
	}],
	"selectedCAN":{
		"canName":"string",
		"canId":"string"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## removeById

*作者: yWX970191*

**请求URL**

/analysisOnLine/removeById `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## removeById

*作者: yWX970191*

**请求URL**

/analysisOnLine/findAll `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询信号列表

*作者: yWX970191*

**请求URL**

/analysisOnLine/findAll `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## pullData

*作者: yWX970191*

**请求URL**

/analysisOnLine/pullData `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## pullDataPlayBack

*作者: yWX970191*

**请求URL**

/analysisOnLine/pullDataPlayBack `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## writeLucene

*作者: yWX970191*

**请求URL**

/analysisOnLine/writeLucene `GET` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## findLucene

*作者: yWX970191*

**请求URL**

/analysisOnLine/findLucene `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## addGroup

*作者: yWX970191*

**请求URL**

/analysisOnLine/addGroup `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## findAllGroup

*作者: yWX970191*

**请求URL**

/analysisOnLine/findAll `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## addSignalByGroup

*作者: yWX970191*

**请求URL**

/analysisOnLine/addBatch `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## deleteGroup

*作者: yWX970191*

**请求URL**

/analysisOnLine/deleteGroup `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 配置：车型控制类
## 车型新增

*作者: ZWX1083624*

**请求URL**

/vehicle/add `POST` 

**请求体**

```json
{
	"vehicleName":"string【必须】",
	"vehicleCId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型信息列表查询

*作者: ZWX1083624*

**请求URL**

/vehicle/select `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型删除

*作者: ZWX1083624*

**请求URL**

/vehicle/delete `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型详细信息查询

*作者: ZWX1083624*

**请求URL**

/vehicle/selectDetail `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型ODX文件导入

*作者: ZWX1083624*

**请求URL**

/vehicle/import `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"odxFilePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型协议增加

*作者: ZWX1083624*

**请求URL**

/vehicle/addVehicleProtocol `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"protocolName":"string //手动添加的协议名称 目前只支持Ethernet、CAN、CANFD【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型协议删除

*作者: ZWX1083624*

**请求URL**

/vehicle/deleteVehicleProtocol `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"protocolId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型部件信息列表查询

*作者: ZWX1083624*

**请求URL**

/vehicle/selectVehicleEcu `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取车型部件详细信息

*作者: ZWX1083624*

**请求URL**

/vehicle/selectVehicleEcuDetail `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"ecuId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型部件增加

*作者: ZWX1083624*

**请求URL**

/vehicle/addVehicleEcu `POST` 

**请求体**

```json
{
	"ecuName":"string【必须】",
	"ecuCId":"string【必须】",
	"vehicleId":"string【必须】",
	"testerIp":"string【必须】",
	"testerEthernetLogicalAddress":"string【必须】",
	"testerCanLogicalAddress":"string【必须】",
	"ecuIp":"string【必须】",
	"ethernetLogicalAddress":"string【必须】",
	"ethernetFunctionAddress":"string【必须】",
	"canLogicalAddress":"string【必须】",
	"canFunctionAddress":"string【必须】",
	"isTwoLayerForward":"int //是否支持二层转发 0-不支持，1-支持（父部件，可以通过Ethernet直连）【必须】",
	"parentEcuId":"string //子部件传父部件Id,父部件传空字符串【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型部件删除

*作者: ZWX1083624*

**请求URL**

/vehicle/deleteVehicleEcu `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"ecuId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 车型部件信息变更

*作者: ZWX1083624*

**请求URL**

/vehicle/updateVehicleEcu `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"ecuId":"string【必须】",
	"testerIp":"string",
	"testerEthernetLogicalAddress":"string",
	"testerCanLogicalAddress":"string",
	"ecuIp":"string",
	"ethernetLogicalAddress":"string",
	"ethernetFunctionAddress":"string",
	"canLogicalAddress":"string",
	"canFunctionAddress":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件相关文件上传

*作者: ZWX1083624*

**请求URL**

/vehicle/uploadEcuFile `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】",
	"fileType":"int //上传文件类型，0-安全算法，1-TLS认证设备证书文件，2-TLS认证设备证书私钥【必须】",
	"filePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件相关文件删除

*作者: ZWX1083624*

**请求URL**

/vehicle/clearEcuFile `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】",
	"fileType":"int //上传文件类型，0-安全算法，1-TLS认证设备证书文件，2-TLS认证设备证书私钥【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件支持协议变更

*作者: ZWX1083624*

**请求URL**

/vehicle/updateEcuProtocol `POST` 

**请求体**

```json
{
	"vehicleId":"string【必须】",
	"ecuId":"string【必须】",
	"protocolId":"string【必须】",
	"status":"int //部件协议支持状态，0-停用，1-启用【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询部件扩展信息

*作者: ZWX1083624*

**请求URL**

/vehicle/selectEcuExtend `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 变更部件扩展信息

*作者: ZWX1083624*

**请求URL**

/vehicle/updateEcuExtend `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】",
	"isFlashBoot":"int //是否支持Boot刷写，0-不支持，1-支持【必须】",
	"isSecretKey":"int //是否支持秘钥灌装，0-不支持，1-支持【必须】",
	"isSupportABChange":"int //是否支持AB区切换，0-不支持，1-支持【必须】",
	"isSupportCertWrite":"int //是否支持证书灌装，0-不支持，1-支持【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询部件诊断仪在线信息参数

*作者: ZWX1083624*

**请求URL**

/vehicle/selectDiagnosticConfig `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】",
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 变更部件诊断仪在线信息参数

*作者: ZWX1083624*

**请求URL**

/vehicle/updateDiagnosticConfig `POST` 

**请求体**

```json
{
	"ecuId":"string【必须】",
	"vehicleId":"string【必须】",
	"periodTime":"int //诊断仪在线发送周期/秒【必须】",
	"addressingMode":"int //诊断仪在线寻址方式 0-功能寻址，1-物理寻址【必须】",
	"responseMode":"int //诊断仪在线响应模式 0-抑制响应，1-需要响应【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 部件下挂

*作者: ZWX1083624*

**请求URL**

/vehicle/changeEcuToChild `POST` 

**请求体**

```json
{
	"ecuIdParent":"string【必须】",
	"ecuIdChild":"string【必须】",
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 解除部件下挂

*作者: ZWX1083624*

**请求URL**

/vehicle/removeEcuParentId `POST` 

**请求体**

```json
{
	"ecuIdParent":"string【必须】",
	"ecuIdChild":"string",
	"vehicleId":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 版本查询
## 领域/子领域/产品列表查询接口

**

**请求URL**

/version/queryProduct `POST` 

**请求体**

```json
{
	"parentId":"string【必须】",
	"level":"string【必须】",
	"lang":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询版本列表接口

**

**请求URL**

/version/queryVersionByProduct `POST` 

**请求体**

```json
{
	"productId":"string //产品Id【必须】",
	"productName":"string //产品名称【必须】",
	"versionName":"string //版本名称",
	"startTime":"string //最后更新时间-开始时间 例：1648785237248",
	"endTime":"string //最后更新时间-结束时间 例：1648785237248",
	"pageInfo":{
		"pageSize":"int //分页大小",
		"pageIndex":"int //当前页"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据版本信息查询文件列表接口

**

**请求URL**

/version/queryFileList `POST` 

**请求体**

```json
{
	"versionId":"string //版本Id【必须】",
	"versionName":"string //版本名称【必须】",
	"productName":"string【必须】",
	"pageInfo":{
		"pageSize":"int //分页大小",
		"pageIndex":"int //当前页"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 通用消息/白名单消息查询接口

**

**请求URL**

/version/queryVersionByProductLine `POST` 

**请求体**

```json
{
	"productLineId":"string //产品线ID【必须】",
	"lang":"string //语言",
	"startTime":"string //最后更新时间-开始时间【必须】",
	"endTime":"string //最后更新时间-结束时间",
	"pageInfo":{
		"pageSize":"int //分页大小",
		"pageIndex":"int //当前页"
	}
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 文件下载

**

**请求URL**

/version/fileDownLoad `POST` 

**请求体**

```json
{
	"id":"long //文件id",
	"downloadStatus":"string //下载状态  枚举值：0：下载中；1：已完成；2：下载失败；3、已删除",
	"srcFileDownloadUrl":"string【必须】",
	"srcSize":"long",
	"sigFileDownloadUrl":"string【必须】",
	"sigSize":"long",
	"savePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
# 日志分析：VHR解析控制类
## 添加文件视图

**

**请求URL**

/vhrAnalysis/addFileTree `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
filePath|string|否|filePath
ecuName|string|否|ecuName

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询文件视图

**

**请求URL**

/vhrAnalysis/selectFileTree `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
ecuName|string|否|ecuName

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 刷新文件视图

**

**请求URL**

/vhrAnalysis/refreshFileTree `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
ecuName|string|否|ecuName

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除文件视图

**

**请求URL**

/vhrAnalysis/deleteFileTree `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
id|string|否|id
ecuName|string|否|ecuName
filePath|string|否|filePath

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 解析文件

**

**请求URL**

/vhrAnalysis/vhrAnalysisTable `POST` 

**请求体**

```json
{
	"fileId":"string //文件视图ID",
	"ecuName":"string //部件",
	"edition":"string //版本",
	"parseFilePath":"string //解析文件地址",
	"contrastFilePath":"string //解析对照json文件地址"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 信号树数据

**

**请求URL**

/vhrAnalysis/signalTreeList `POST` 

**请求体**

```json
{
	"contrastFilePath":"string",
	"parseFilePath":"string",
	"id":"string",
	"ecuName":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 获取部件列表

**

**请求URL**

/vhrAnalysis/getParts `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 根据部件获取版本信息

**

**请求URL**

/vhrAnalysis/getEditionByParts `POST` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
part|string|否|part

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## VHR分析数据分页查询

**

**请求URL**

/vhrAnalysis/vhrAnalysisPageQuery `POST` 

**请求体**

```json
{
	"ecuName":"string //部件名称【必须】",
	"edition":"string //版本【必须】",
	"parseFilePath":"string //解析文件地址【必须】",
	"logTypes":"string[]",
	"logLevels":"string[]",
	"eventIds":"string[]",
	"startTime":"string",
	"endTime":"string",
	"pageSize":"int //每页显示个数【必须】",
	"curPage":"int //当前页【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询VHR分析查询条件

**

**请求URL**

/vhrAnalysis/vhrAnalysisConditionQuery `POST` 

**请求体**

```json
{
	"ecuName":"string //部件名称【必须】",
	"edition":"string //版本【必须】",
	"parseFilePath":"string //解析文件地址【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 导出VHR日志解析结果

**

**请求URL**

/vhrAnalysis/vhrAnalysisExport `GET` 

**请求参数**

参数名|类型|必须|描述
--:|:--:|:--:|:--
ecuName|string|是|部件名称
edition|string|是|版本
parseFilePath|string|是|解析文件地址
logTypes|string[]|否|
logLevels|string[]|否|
eventIds|string[]|否|
startTime|string|否|
endTime|string|否|

**返回结果**

```json
{}
```
# 日志分析：VHR_CAN解析控制类
## 添加文件视图

**

**请求URL**

/vhrCanAnalysis/addCanFileTree `POST` 

**请求体**

```json
{
	"filePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询文件视图

**

**请求URL**

/vhrCanAnalysis/selectCanFileTree `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 刷新文件视图

**

**请求URL**

/vhrCanAnalysis/refreshCanFileTree `POST` 


**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 删除文件视图

**

**请求URL**

/vhrCanAnalysis/deleteCanFileTree `POST` 

**请求体**

```json
{
	"id":"string【必须】",
	"filePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 解析can报文

**

**请求URL**

/vhrCanAnalysis/vhrCanAnalysisTable `POST` 

**请求体**

```json
{
	"fileId":"string //文件视图ID",
	"parseCanFilePath":"string //解析文件地址",
	"dbcFilePath":"string //解析对照json文件地址"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 信号树数据

**

**请求URL**

/vhrCanAnalysis/canSignalTreeList `POST` 

**请求体**

```json
{
	"dbcFilePath":"string",
	"parseCanFilePath":"string",
	"id":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## can报文分析数据分页查询

**

**请求URL**

/vhrCanAnalysis/vhrCanAnalysisPageQuery `POST` 

**请求体**

```json
{
	"parseCanFilePath":"string //解析文件地址【必须】",
	"pageSize":"int //每页显示个数【必须】",
	"curPage":"int //当前页【必须】",
	"startTime":"string",
	"endTime":"string",
	"versions":"string[]",
	"alarmTypeNames":"string[]",
	"vinCode":"string"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
## 查询can报文分析查询条件

**

**请求URL**

/vhrCanAnalysis/vhrCanAnalysisConditionQuery `POST` 

**请求体**

```json
{
	"parseCanFilePath":"string【必须】"
}
```

**返回结果**

```json
{
	"message":"string",
	"code":"string",
	"data":"object"
}
```
